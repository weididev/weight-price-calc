name: Build Android App (APK & AAB)

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Install Dependencies
        run: npm install

      - name: Build Web Code
        run: npm run build

      # Initialize Android Project
      - name: Initialize Android
        run: |
          npm install @capacitor/android
          npx cap add android
          npx cap sync

      # Inject AdMob App ID (Using Real Production ID)
      - name: Configure AdMob in Manifest
        run: |
          sed -i 's|</application>|        <meta-data android:name="com.google.android.gms.ads.APPLICATION_ID" android:value="ca-app-pub-2018579775913448~9911402503"/>\n    </application>|' android/app/src/main/AndroidManifest.xml

      # Build both APK and AAB (Debug versions)
      - name: Build APK and AAB
        run: |
          cd android
          ./gradlew assembleDebug bundleDebug

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Android-Builds
          path: |
            android/app/build/outputs/apk/debug/app-debug.apk
            android/app/build/outputs/bundle/debug/app-debug.aab
